add: (x: int, y: int) -> int {
    return x + y 
}


main: () -> int {
    x: int = 2
    y: int = 3
    
    while x < y {
        x = x + 1
    }

    if x > y {
        return 4
    } else {
        return add(x, y) + add(30, 4)
    }
}
