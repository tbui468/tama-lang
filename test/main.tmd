import math
import complex

main: () -> int {
    print(true and false)
    x: int = 0
    y: int = 5
    
    z: int = sub(x, y)
    while x < y {
        x = x + 1
    }

    if x > y {
        print(0)
    } else {
        print(add_complex() + 100)
    }

    return 0
}
