add: (a: int, b: int) -> int {
                            return a + b
                        }
                        main: () -> int {
                            x: int = add(-9, 9)
                            return x
                        }