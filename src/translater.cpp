#include "translater.hpp"


void Translater::emit_asm(const std::string& input, const std::string& output) {
    /*
        read(input);
        lex();
        parse();
        translate();
        write(output);*/
}
